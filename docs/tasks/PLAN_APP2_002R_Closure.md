# APP2_002R 窄范围收尾计划

依据 REVIEW_APP2_002_V1_FACILITY_OMEGA，基线 9c355d4efadcd08160262902bc7793933d863b32。

1. R1/R2：有符号区间使用64位差值与偏移；覆盖跨零、全i32范围、固定seed和XP散布。
2. R3/R4：每越过阈值累积 pending_levelups；一次选择扣一个，重新生成下一组选项，直到清零恢复。保留旧人工fixture只设置bool的单次面板兼容入口；自然升级必须计数。
3. 合并已完成的1/2/3输入修复；数字键不使用Win32自动重复连续消耗选择。实现Facility P暂停/R重置并覆盖状态、视口、相机、队列复位。
4. R5/R6：专项、完整Golden/gpu2d回归；重新记录因RNG与hash字段变化而变化的确定性证据，不改Golden期望像素。
5. R7/R8：V3突出范围效果、V4单个Elite焦点、showcase手工编排战斗构图；继续实际生产渲染，独立像素与状态证据。输出到app2_002r，保留前轮历史证据。
6. R9：按审查要求在验证后创建本地实现提交，报告直接记录该实现的完整SHA作为END_COMMIT，再单独提交报告。此流程不推送远端；视觉审批仍为PENDING，不能自授最终PASS。

不实施A2/A3性能重构，不新增武器、地图、GPU语义或RTL。
