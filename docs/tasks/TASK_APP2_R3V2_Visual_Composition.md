# TASK APP2 R3V2 — 地图视觉构图完善

日期：2026-09-16；基线：3e97372。授权：用户要求依照 REVIEW_APP2_MAP_R3_M13_VISUAL_V1 规划并实施。

## 范围与顺序

1. 保持 Hero Area 的 12 个碰撞矩形、四个区域、角色移动及世界尺寸不变。新增回归锁定几何坐标；保留已有断言。
2. 复用不可变源图和现有离线资源编译器，提供 3 种低对比度地面材质；不增加依赖。
3. 用现有 GPU API 绘制非碰撞地面细节：中断/错位接缝、检修盖板、线缆槽、排水沟、方向标记、磨损和装卸标识。细节按区域用途组织，保持中央战斗区开阔。
4. 将设备连接到墙体或地面管线；减弱完整高亮台座边框。墙体增加内侧沟槽、转角、支撑、门口端帽和局部灯，装饰不扩大碰撞语义。
5. 维修用黄铜色与管线，仓储用暖色装卸框，动力区用青色技术线路和光池，大厅用测试标识与低对比检修地面。
6. 新增独立受控展示 fixture：20 个敌人（含 Tank）、多发子弹、XP、透明光晕和加法特效，全部使用生产渲染器。标明摆拍性质；同时保留真实 180 帧空闲输入截图，不混淆。
7. 输出到独立 map_r3v2 目录：总览、碰撞图、三屏、滚动、正常 gameplay_180 和 showcase。检查真实截图，运行地图/展示验证及完整 CTest，记录准确命令与限制。

## 边界与验收

不改变 GPU/Tile/Immediate 语义、碰撞解算、武器、升级或进度。装饰像素不推导碰撞。展示场景是验证/宣传素材，不是新增玩法。M13 必须由用户视觉审阅，自动测试不授予通过；最终报告明确保持 HOLD / PENDING。当前任务不包含 Git 提交或推送。
