# Reports

Agent execution reports live here.

Naming convention:

```text
REPORT_<NNN>_<Title>.md
```

Each report must cover files changed, commands run, test results, blockers, design questions, and suggested next steps per `AGENTS.md`.
